name = "ansury-system"
compatibility_date = "2024-01-01"
compatibility_flags = [ "nodejs_compat" ]

[vars]
# Add your non-secret variables here
APP_URL = "https://your-app.pages.dev"

# Secrets (API Keys) should be added via the Cloudflare Dashboard
# or using `wrangler secret put KEY_NAME`
